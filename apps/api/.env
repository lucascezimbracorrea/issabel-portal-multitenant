PORT=8787

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=Mudar123@@**
DB_NAME=issabel-portal

# JWT — gerar com: openssl rand -base64 48
JWT_SECRET=change-this-to-a-random-secret-min-32-chars-before-production!!

# CORS — separar por vírgula para múltiplos domínios
CORS_ORIGINS=http://localhost:5173,http://localhost:4173

# Telephony internal token
INTERNAL_TELEPHONY_TOKEN=change-me-in-production
