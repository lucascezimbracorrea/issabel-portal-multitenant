/** Default one PBX mark (PNG). Override per-tenant via `appearance.logoUrl` where supported. */
export const ONE_PBX_LOGO_URL = '/logo.png';

/** Login left panel — same hero as hotel-hub-custom (onetelecom). */
export const LOGIN_SIDE_BACKGROUND_URL =
  'https://onetelecom.net.br/wp-content/uploads/2024/10/banner-003.webp';

/** Login left panel default logo (onetelecom), when host branding has no logoUrl. */
export const LOGIN_DEFAULT_BRAND_LOGO_URL =
  'https://onetelecom.net.br/wp-content/uploads/2024/10/logo-new.webp';
